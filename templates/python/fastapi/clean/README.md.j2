# {{ project_name }}

Generated by Forge as a **{{ framework_label }}** REST API using
**{{ architecture_label }}**.

## Stack

- Language: Python
- Framework: {{ framework_label }}
- Architecture: {{ architecture_label }}
{% if database %}
- Database: {{ database_engine }}
- ORM: {{ orm }}
{% if migrations %}
- Migrations: Alembic
{% endif %}
{% endif %}
{% if docker %}
- Docker: yes
{% endif %}
{% if testing %}
- Tests: pytest
{% endif %}
{% if linting %}
- Linting: Ruff
{% endif %}

## Setup

```bash
uv sync
```

{% if database or docker %}
## Configuration

```bash
cp .env.example .env
```

{% if database %}
| Variable | Purpose |
|----------|---------|
| `DATABASE_URL` | SQLAlchemy database URL |
{% endif %}
{% endif %}

## Run

```bash
{{ run_command }}
```

Health check: `GET /api/health`.

{% if migrations %}
## Migrations

```bash
uv run alembic revision --autogenerate -m "initial"
uv run alembic upgrade head
```
{% endif %}

{% if testing %}
## Tests

```bash
uv run pytest
```
{% endif %}

{% if linting %}
## Lint

```bash
uv run ruff check .
```
{% endif %}

{% if docker %}
## Docker

{% if is_postgresql and database %}
```bash
docker compose up -d db
docker compose up --build
```
{% else %}
```bash
docker compose up --build
```
{% endif %}
{% endif %}

## Layout

Clean Architecture packages under `src/{{ package_name }}/`:

- `domain/` — pure business concepts (no framework imports)
- `application/` — use cases{% if database %}; `ports.py` defines persistence ports{% endif %}
- `infrastructure/` — config{% if database %}, SQLAlchemy session, persistence adapters{% endif %}
- `presentation/` — FastAPI HTTP routes
- `main.py` — application entrypoint

Dependency rule: `presentation` → `application` → `domain`;
`infrastructure` implements ports and is wired at the edges.
