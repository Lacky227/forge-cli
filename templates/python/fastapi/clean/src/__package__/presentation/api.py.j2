from fastapi import APIRouter

from {{ package_name }}.application.health import get_health_status

router = APIRouter(tags=["health"])


@router.get("/health")
def health() -> dict[str, str]:
    status = get_health_status()
    return {"status": status.status}
