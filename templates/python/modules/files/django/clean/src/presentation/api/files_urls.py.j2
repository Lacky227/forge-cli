from rest_framework.routers import DefaultRouter

from presentation.api.file_views import FileViewSet

router = DefaultRouter()
router.register(r"files", FileViewSet, basename="file")

urlpatterns = router.urls
